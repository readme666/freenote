version: 1

project:
  name: freenote
  version_command: git describe --tags --abbrev=0

scripts:
  pre_build:
    - mkdir translations-build
    - cd translations-build
    - cmake "${PROJECT_ROOT}"
    - make translations
    - cd ..

build:
  cmake:

appimage:
  linuxdeploy:
